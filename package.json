{
  "name": "cloud-study-assistant",
  "private": true,
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"cd backend && uvicorn main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "dev:fe": "cd frontend && npm run dev",
    "dev:be": "cd backend && uvicorn main:app --reload --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
